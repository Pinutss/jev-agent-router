---
name: jev-agent-router
description: Select an agent from a registry with agent_route.
---

# JEV Agent Router

Use the `agent_route` tool from the `jev-agent` MCP server.

Required: task, agents.

Never pass API keys in the tool arguments.
